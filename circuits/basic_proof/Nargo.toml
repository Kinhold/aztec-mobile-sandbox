[package]
name = "identity_verification"
type = "bin"
authors = ["StationaryDev37"]
compiler_version = "0.30"
description = "Production-grade identity verification circuit using Poseidon hashing for Aztec PXE"

[dependencies]
std = { tag = "v0.30.0" }

[profile.dev]
# Development profile: faster compilation, less optimization
opt_level = 0

[profile.release]
# Release profile: optimized for proof generation
opt_level = 2
